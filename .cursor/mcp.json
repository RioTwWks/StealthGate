{
  "mcpServers": {
    "stealth-gate": {
      "command": "cargo",
      "args": [
        "run",
        "--quiet",
        "--bin",
        "stealth-gate-mcp",
        "--",
        "--config",
        "configs/config.toml"
      ],
      "description": "MCP stdio: статистика, конфиг, reload и смена MTProto secret"
    }
  }
}
